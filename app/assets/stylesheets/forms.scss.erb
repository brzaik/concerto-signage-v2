form fieldset {
  font-size: 100%;
  padding: 10px;
  legend {
    font-size: 150%;
    font-weight: 400;
    color: #000;
    padding: 0 5px; } }

label {
  font-size: 150%;
  u {
    font-style: normal;
    text-decoration: underline; } }

input, select, textarea {
  background: #ccc;
  border: solid 1px #999;
  font-family: "HelveticaNeue-Light","Helvetica Neue Light","Helvetica Neue",Helvetica,sans-serif;
  font-size: 1em;
  color: #000; }

select {
  width: auto!important; }

textarea {
  font-family: Courier, monospace;
  font-size: .8em;
  overflow: auto;
  height: 300px; }

form {
  div {
    clear: left;
    margin: 5px 0 0;
    padding: 1px 3px; }
  fieldset div.notes {
    float: right;
    width: 100%;
    height: auto;
    margin: 0 0 10px 10px;
    padding: 5px;
    border: 1px solid #666;
    background-color: #ffffe1;
    color: #666;
    font-size: 88%;
    h4 {
      padding: 3px 0 3px 27px;
      color: #666;
      font-size: 110%; }
    p {
      margin: 0 0 1.2em;
      color: #666;
      &.last {
        margin: 0; } } }
  div {
    fieldset {
      clear: none;
      margin: 0 0 0 142px;
      padding: 0 5px 5px;
      legend {
        font-size: 1em;
        padding: 0 3px 0 9px; } }
    &.required fieldset legend {
      font-weight: 700; }
    label {
      float: left;
      width: 150px;
      background-color: #e0e0e0;
      font-size: 16px;
      padding: 3px 5px;
      margin: 0 0 5px;
      text-align: right;
      font-weight: 700; }
    &.optional label {
      font-weight: 400; } } }

label.optional {
  font-weight: 400; }

form div.required label, label.required {
  font-weight: 700; }

form div {
  label {
    &.labelCheckbox, &.labelRadio {
      float: none;
      margin: 0 0 5px 142px;
      text-align: left; } }
  fieldset label {
    &.labelCheckbox, &.labelRadio {
      margin: 0 0 5px; } } }

p.error {
  color: #DB8101;
  background-color: #DBA941;
  font-size: 14px;
  padding: 1em; }

form div {
  input, select, textarea {
    width: 80%;
    padding: 6px;
    margin: 0; }
  input.inputFile {
    width: 211px; }
  select {
    &.selectOne, &.selectMultiple {
      width: 211px;
      padding: 1px 3px; } }
  input {
    &.inputCheckbox, &.inputRadio {
      display: inline;
      height: 14px;
      width: 14px;
      background-color: transparent;
      border-width: 0; } } }

input {
  &.inputCheckbox, &.inputRadio {
    display: inline;
    height: 14px;
    width: 14px;
    background-color: transparent;
    border-width: 0; } }

form div.submit {
  padding: 0 0 0 125px;
  clear: both; }

div.submit input {
  background: #333;
  border: solid 1px #000;
  color: #FFF;
  padding: 2px 12px;
  text-decoration: none;
  font-size: 14px;
  font-weight: 700;
  text-align: center;
  text-decoration: none;
  width: auto;
  &:hover {
    padding: 2px 12px;
    font-size: 14px;
    font-weight: 700;
    text-align: center;
    text-decoration: none;
    width: auto; } }

form {
  div {
    &.submit div input {
      &.inputSubmit, &.inputButton {
        float: right;
        margin: 0 0 0 5px; } }
    small {
      margin: 0 0 5px 142px;
      padding: 1px 3px;
      font-size: 88%; } }
  fieldset legend {
    line-height: 150%; }
  input, select, textarea {
    background-color: #fff; } }

/* form.import.css */

div {
  &.optional label:before, &.required label:before {
    content: ''; } }

form div label {
  &.labelCheckbox, &.labelRadio {
    width: 190px;
    padding: 4px 0 0 18px;
    text-indent: -18px;
    line-height: 120%; } }

label {
  &.labelCheckbox, &.labelRadio {
    width: 190px;
    padding: 4px 0 0 18px;
    text-indent: -18px;
    line-height: 120%; } }

form div label {
  &.labelCheckbox input.inputCheckbox, &.labelRadio input.inputRadio {
    margin: 0; } }

label {
  &.labelCheckbox input.inputCheckbox, &.labelRadio input.inputRadio {
    margin: 0; } }

form div {
  fieldset {
    input {
      &.inputText, &.inputPassword, &.inputFile {
        width: 100%;
        margin: 0 0 0 18px; } }
    textarea.inputTextarea {
      width: 100%;
      margin: 0 0 0 18px; } }
  label.compact {
    display: inline;
    width: auto;
    padding: 4px 10px 0 0;
    text-indent: 0;
    margin: 0; }
  &.wide label {
    float: none; }
  label.wide {
    width: 348px; }
  input.smallinput {
    width: 150px!important; }
  &.wide {
    input {
      &.inputText, &.inputPassword, &.inputFile {
        width: 100%;
        margin: 0; } }
    select, textarea {
      width: 100%;
      margin: 0; } }
  &.notes p, small {
    line-height: 125%; }
  &.wide small {
    margin: 0 0 5px; } }

div.date select, select.autoWidth {
  width: auto; }

option {
  padding-left: 1em; }